Nationstech.N32WB452_Library.1.2.0版本：
1、 SPI 新增 SPI_FLASH_DMA、SPI_RECORDER 例程
2、 USB 新增 Audio_Speaker、HID_Keyboard_TwoWay 和 Printer例程
3、 公共文件n32wb452.h v1.0.2
        修改RCC_CFG_USBPRES_PLLDIV3、RCC_CFG_USBPRES_PLLDIV2的定义
        修改RCC_APB1ENR_OPAMPEN宏定义名称
        修改RCC_CSR_MMURSTF宏定义名称
        修改FLASH_AC_LATENCY的宏定义值
        删除TSC模块中备用的旧寄存器定义
4、 公共文件log.c、log.h（V1.0.1） 修改调试UART端口为PA9/PA10
5、 驱动n32wb452_rcc.c、n32wb452_rcc.h（v1.0.2）中：
        更新RC_HSE_DISABLE宏定义名称
        更新RCC_ADC1MCLK_SRC_HSE宏定义的值
6、 驱动n32wb452_flash.c、n32wb452_flash.h（v1.0.2）中：
        更新FLASH_SetSMPSELStatus函数定义
        更新FLASH_GetSMPSELStatus函数定义
        删除RCC_ConfigEthClk相关定义
7、 驱动n32wb452_adc.c、n32wb452_adc.h(V1.0.2)中：
        修改了ADC_SetDifChsEnable函数名称；
        修改了ADC 模式寄存器位定义名称；
8、 驱动n32wb452_tsc.c、n32wb452_tsc.h(V1.0.2)优化，兼容触控算法库，相应优化TSC_Lib_demo例程
9、优化了RTC TimeStamp例程
10、驱动n32wb452_tim.c(V1.0.2)，改变TIM_EnableOc1Preload函数为TIM_ConfigOc1Preload；增加TIM_GetAutoReload、TIM_GetCCENStatus函数
11、增加IAR工程示例
12、增加适用于IAR的蓝牙库
13、I2C模块优化：
    驱动n32g45x_i2c.c 修改I2C初始化函数
    所有DEMO修改I2C端口配置,更新readme
14、更新蓝牙驱动库及例程
15、 ADC驱动C文件增加对校准功能的函数重入性控制

Nationstech.N32WB452_Library.1.1.0版本：
1、示例代码Flash_Write_Protectio工程nmain.c(V1.0.1)，清除标志作了修改；
2、示例代码Flash_Program工程main.c(V1.0.1)，错误处理作了修改；
3、示例代码Flash_DMA_Program工程main.c(V1.0.1)，清除标志作了修改；
4、驱动n32wb452_flash.c(V1.0.1)，擦写操作增加先清除标志，然后再进行后续操作；
5、驱动n32wb452_adc.c(V1.0.1),增加void ADC_SetDifChsEnable(ADC_Module* ADCx,uint32_t DifChs)；
6、触控算法库n32wb452_touch_app.c，
    1)const TSC_AlgInitThreValue gtsc_list[]做了修改；
    2)int32_t tsc_alg_isr_callback(tsc_alg_type type, uint32_t event, uint32_t chn, uint32_t value)函数作了修改；
    3)int32_t app_touch_init(void)函数做了修改；
    4)static void n32wb452_touch_normal(void)函数作了修改；
7、触控算法库n32wb452_rtc_hal.c，void WakeUpClockSelect(uint8_t WKUPClkSrcSel)函数作了修改；
8、devBoardDemo示例代码n32wb452_touch_app.c，
    1)const TSC_AlgInitThreValue gtsc_list[]作了修改；
    2)int32_t tsc_alg_isr_callback(tsc_alg_type type, uint32_t event, uint32_t chn, uint32_t value)函数作了修改；
    3)int32_t app_touch_init(void)函数作了修改；
    4)static void n32wb452_touch_normal(void)函数作了修改；
9、驱动n32wb452_tim.h(V1.0.1)，TIM_BDTRInitType结构体修改注释；
10、驱动n32wb452_rcc.h(V1.0.1)，
   1)修改了TRNG1MCLK分频的宏定义；
   2)修改了IS_RCC_TRNG1MCLKPRE(VAL)宏定义；
11、驱动n32wb452_gpio.h(V1.0.1),修改了IS_GPIO_REMAP(REMAP)宏定义；
12、驱动n32wb452_flash.h(V1.0.1),
   1)修改了FLASH_ST枚举定义；
   2)删除FLASH_INT_ECCERRIE宏定义；
   3)修改IS_FLASH_INT(IT)宏定义；
   4)删除FLASH_FLAG_ECCERR宏定义；
   5)修改了IS_FLASH_CLEAR_FLAG宏定义；
   6)修改IS_FLASH_GET_FLAG(FLAG宏定义；
   7)增加了FLASH_STS_CLRFLAG宏定义；
13、驱动n32wb452_adc.h(V1.0.1)，
   1)修改了ADC_DIFSEL_CHS_MASK宏定义名称；
   2)删除ADC_DIFSEL_CHS_0宏定义；
   3)增加ADC_CTRL3_PDRDY_MSK、ADC_CTRL3_RDY_MSK宏定义；
   4)修改了ADC_CTRL3_RES枚举定义；
   5)修改了ADC_InitTypeEx结构体；
   4)增void ADC_SetDifChs(ADC_Module* ADCx,uint32_t DifChs)声明；
   5)增void ADC_ConfigClk(ADC_CTRL3_CKMOD ADC_ClkMode, uint32_t RCC_ADCHCLKPrescaler)声明；
14、驱动n32wb452.h(V1.0.1)，
    1)FLASH_Module结构体将ECCR变量改为RESERVED2；  
    2)QSPI_Module结构体变量DAT32~DAT35改为RESERVED2~RESERVED5；
    3)增加Bit definition for ADC_DIFSEL register宏定义；
    4)增加Bit definition for ADC_CALFACT register宏定义；
    5)增加Bit definition for ADC_CTRL3 register宏定义；
    6)增加Bit definition for ADC_SAMPT3 register宏定义；
    7)删除FLASH_AC_LATENCY_5、FLASH_STS_ECCERR、FLASH_CTRL_ECCERRITEFLASH_ECCR_ECCLW_MSK、FLASH_ECCR_ECCHW_MSK宏定义；
    8)修改了HSE_STARTUP_TIMEOUT宏定义值;
    9)修改了CAN模块寄存器定义结构体;
    10)ADC_Module结构体去除IPTST变量；
    11)修改ADC_Module结构体里面的 WDHIGH
16、驱动n32xx_tsc_alg_api.h(V1.0.1)，
    1)删除TSC_ALG_RESERVE0宏定义；
    2)增加TSC_ALG_DEBUG_BUF_SIZE、TSC_ALG_REF_SIZE_PER_CHN宏定义；
    3)修改tsc_press_key_event_e枚举定义；
    4)删除calib_level_t枚举定义；
    5)修改tsc_ret_code_e枚举定义；
    6)修改tsc_hld_lev_e枚举定义；
    7)增加tsc_delta_limit_lev_e枚举定义；
    8)增加tsc_resist_disturb_lev_e枚举定义；
    9)修改TSC_AlgInitThreValue_t结构体定义；
17、更新了n32xx_tsc_alg_api.lib 
18、更新USART工程文件及配置文件（删除未使用的驱动文件），硬件流控Receive_RTS工程删除“数据比对时接收Buffer的1字节偏移”，多处理器模式工程更新LED为PB5/PB4/PA8
19、更新USART IrDA红外解码功能38kHz载波生成方式，由TIM中断翻转IO改为TIM PWM输出，否则影响IrDA输出数据
20、新增单线半双工模式DMA示例DMA_HalfDuplex
21、TSC保留旧版寄存器定义，以宏区分，默认为新的定义方式
22、因N32WB452 不支持XFMC,修改n32wb452.h，删除XFMC相关信息，同时删除文件n32wb452_xfmc.c、n32wb452_xfmc.h以及XFMC DEMO文件夹
23、新增I2C模块10bit地址读写例程
24、USB 新增 Mass_Stroage_DataFlash 例程
25、USB 新增 Virtural_Com_Port 例程
26、I2C 删除两个多余从设备的例程，同时调整例程为可在同一块板上做验证
27、DMA 的I2C_DMA例程调整端口
28、修改CAN1对应GPIO remap定义
29、USB 新增 JoyStickMouse 例程
30、驱动n32wb452_tim.c(V1.0.2)，改变TIM_EnableOc1Preload函数为TIM_ConfigOc1Preload；增加TIM_GetAutoReload、TIM_GetCCENStatus函数；
31、USB 例程修改字符描述符
32、SPI 新增 SPI_FLASH_DMA 例程
33、USB 新增 HID_Keyboard_TwoWay 例程
34、USB 新增 Printer 打印机例程

